export const CART_CHANGED_EVENT = "vitrina-cart-changed";

export const SHOP_ROUTES = {
  cart: "/cart",
  checkout: "/checkout",
} as const;

export const SHOP_API = {
  cart: "/api/cart",
  checkout: "/api/checkout",
} as const;
